interface:
  display_name: "PR Review"
  short_description: "Review a GitHub PR with reviewer-agent support"
  default_prompt: "Use $pr-review to inspect a GitHub pull request and run a reviewer subagent for concrete merge-blocking findings."
