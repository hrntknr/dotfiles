name: default
k8s: true
rules:
  - allow(rw, ${WORK_DIR})
  - allow(rw, ~/.claude)
  - allow(rw, ~/.claude.json)
  - allow(rw, ~/.codex)
  - allow(rw, ~/.ssh/known_hosts)
  - allow(r, ~/.gitconfig)
  - deny(rw, ~/.*)
  - allow(r, /)
