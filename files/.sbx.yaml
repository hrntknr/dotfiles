name: default
k8s: true
rules:
  - allow(rw, ${WORK_DIR})
  - allow(r, ~/.local)
  - allow(r, ~/.gitconfig)
  - allow(rw, ~/.ssh/known_hosts)
  - allow(rw, ~/.claude)
  - allow(rw, ~/.claude-pfn)
  - allow(rw, ~/.claude.json)
  - allow(rw, ~/.codex)
  - allow(rw, ~/work/go/pkg/mod)
  - deny(rw, ~/.*)
  - allow(r, /)
