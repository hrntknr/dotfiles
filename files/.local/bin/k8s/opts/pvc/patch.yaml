apiVersion: ${BASE_API_VERSION}
kind: ${BASE_KIND}
metadata:
  name: ${NAME}
spec:
  template:
    spec:
      containers:
        - name: ${NAME}
          volumeMounts:
            - mountPath: /root/work
              name: work
      volumes:
        - name: work
          persistentVolumeClaim:
            claimName: ${PVC_NAME}
