apiVersion: ${BASE_API_VERSION}
kind: ${BASE_KIND}
metadata:
  name: ${NAME}
spec:
  template:
    spec:
      containers:
        - name: ${NAME}
          resources:
            limits:
              nvidia.com/gpu: 1
            requests:
              nvidia.com/gpu: 1
