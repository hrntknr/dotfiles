apiVersion: ${API_VERSION}
kind: ${KIND}
metadata:
  name: ${NAME}
spec:
  template:
    spec:
      containers:
        - name: ${PRIMARY_CONTAINER}
          resources:
            limits:
              nvidia.com/gpu: 1
            requests:
              nvidia.com/gpu: 1
