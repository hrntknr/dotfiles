apiVersion: ${API_VERSION}
kind: ${KIND}
metadata:
  name: ${NAME}
spec:
  template:
    spec:
      containers:
        - name: ${PRIMARY_CONTAINER}
          envFrom:
            - secretRef:
                name: ${SECRET_NAME}
